Atomic Notes MD clean build

This clean rebuild preserves:
- PWA manifest and installability
- service worker fallback registration fixes
- icon set and branding
- markdown/Zettelkasten core behavior

It also replaces the layered sidebar patches with a single clean mobile drawer implementation.
