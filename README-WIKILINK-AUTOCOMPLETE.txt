Atomic Notes MD wikilink autocomplete

Added:
- Type [[ in the editor and start typing a note title
- Matching notes appear in a dropdown
- Arrow Up/Down navigates suggestions
- Enter or Tab inserts the selected note as a full wikilink
- Tap/click also inserts the suggestion

Also:
- Service worker cache bumped to v4 so the new editor JS should update after redeploy
